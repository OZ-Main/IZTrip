import { cva } from 'class-variance-authority'

export const tripsPaginationBarRootVariants = cva(
  'flex flex-wrap items-center justify-center gap-3 border-t border-border/60 pt-3 sm:gap-stack sm:pt-form-field',
)
